"""Signal layers for the day-trade CVD divergence strategy.

Deterministic and backtestable by construction: no model calls, no randomness,
same inputs -> same outputs (CLAUDE.md, "Deterministic core").
"""
